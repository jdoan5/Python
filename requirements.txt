# Streamlit Community Cloud dependencies for the Job Search Agent live demo
# (Python Project 3/Job Search Agent/Stage 6/streamlit_app.py).
#
# This file lives at the repo ROOT deliberately: Community Cloud's installer
# passes the requirements path to pip unquoted, so a requirements.txt inside
# "Python Project 3/..." (spaces) breaks the install. Root path has no spaces.
streamlit>=1.50.0
anthropic>=0.77.0
pydantic>=2.7.0
typer>=0.12.0
rich>=13.7.0
httpx>=0.27.0
pyyaml>=6.0
python-dotenv>=1.0.0
