# Loops: for i, x in enumerate(arr)
# arr = ['a', 'b', 'c', 'd', 'e']
# for i, x in enumerate(arr):
#     print(f"Index: {i}, Value: {x}")

# Reverse Loops:
# arr = ['a', 'b', 'c', 'd', 'e']
# for i, x in enumerate(reversed(arr)):
#     print(f"Index: {i}, Value: {x}")

# Slicing
arr = ['a', 'b', 'c', 'd', 'e']
for x in arr[::-1]:
    print(x)
