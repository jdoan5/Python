# Core conversion (main.py / converter.py) needs only the standard library.
# These are for the GUI and the matplotlib rendering fallback:
networkx>=3.0
matplotlib>=3.7
graphviz>=0.20        # Python wrapper; the `dot` binary is optional (see README)

# Packaging into a double-click app (optional):
#   pip install pyinstaller
