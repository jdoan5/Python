# Production-like local run: Django (gunicorn, prod settings) + Postgres.
#
#   cd "Stage 4"
#   DJANGO_SECRET_KEY=$(python3 -c 'import secrets;print(secrets.token_urlsafe(50))') \
#     docker compose up --build
#
# Then http://127.0.0.1:8000 — sign up and use the app backed by Postgres.
# Meal data persists in the pgdata volume across rebuilds.

services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: calorie_tracker
      POSTGRES_USER: tracker
      # Local-compose password; real deployments use managed-DB credentials
      # via DATABASE_URL, never this file.
      POSTGRES_PASSWORD: local-dev-only
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      # -h forces TCP: postgres's first-boot initdb runs a TEMPORARY server on
      # the unix socket only — socket pg_isready can report "healthy" moments
      # before that temp server stops, starting web against a restarting DB.
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -U tracker -d calorie_tracker"]
      interval: 5s
      timeout: 3s
      retries: 10

  web:
    build: .
    ports:
      # Loopback only, same policy as the Job Search Agent compose.
      - "127.0.0.1:8000:8000"
    environment:
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:?generate one and export it (see header comment)}
      DATABASE_URL: postgres://tracker:local-dev-only@db:5432/calorie_tracker
      DJANGO_ALLOWED_HOSTS: localhost,127.0.0.1
      # No TLS locally: skip the SSL redirect AND ship non-Secure cookies —
      # Chrome/Firefox would accept Secure cookies on localhost, but Safari
      # drops them over plain http, which silently breaks login.
      DJANGO_SECURE_COOKIES: "0"
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
    name: calorie_tracker_pgdata
